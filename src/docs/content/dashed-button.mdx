<Steps className ="py-5">


Outlined button with a modern dashed border effect. Clean and minimalist.

<ComponentPreview name="dashedButtonDemo" showLineNumbers>
</ComponentPreview>
### Utils.ts

```tsx
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

```

### Packages

```bash
npm i clsx tailwind-merge motion lucide-react
```
  <ComponentSource
  name="dashedButton"
  title="dashedButton/demo.tsx"
  showLineNumbers
  codeCollapsible
/>
</Steps>



### Props

| Prop      | Type                    | Default | Description                               |
|-----------|-------------------------|---------|-------------------------------------------|
| `text`    | `string`                | `"Hover me"`    | Button text.    |

