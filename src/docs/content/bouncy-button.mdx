<Steps className ="py-5">


A playful button with a bounce animation on hover. 

<ComponentPreview name="bouncyButtonDemo" showLineNumbers>
</ComponentPreview>

### Utils.ts

```bash
npm i clsx tailwind-merge
```

```tsx
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
  <ComponentSource
  name="bouncyButton"
  title="bouncyButton/demo.tsx"
  showLineNumbers
  codeCollapsible
/>
</Steps>


### Packages

```bash
npm i motion
```

