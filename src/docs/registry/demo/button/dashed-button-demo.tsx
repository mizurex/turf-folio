import DashedButton from "../../components/button/dashed-button";

export default function DashedButtonDemo() {
    return (
        <DashedButton onClick={() => alert("BOOOOOOOO")}>
        Hover me
      </DashedButton>
    )
}